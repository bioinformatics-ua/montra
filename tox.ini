[tox]
envlist = py27

[testenv]
commands =  service emif-locust stop
            service emif-locust start
            python emif/manage.py test emif

deps = -r{toxinidir}/requirements.txt

install_command = pip install {opts} {packages} --allow-all-external --allow-unverified pil
